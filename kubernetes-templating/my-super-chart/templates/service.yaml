apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-super-chart.fullname" . }}
  labels:
    {{- include "my-super-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-super-chart.selectorLabels" . | nindent 4 }}
